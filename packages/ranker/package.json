{
  "name": "@ranker/ranker",
  "version": "1.0.0",
  "scripts": {
    "test": "mocha -r ts-node/register tests/**/*.test.ts",
    "build": "npx tsc -b && npm run test",
    "start": "node dist/app.js"
  },
  "dependencies": {
    "@ranker/commons": "^1.0.0",
    "@ranker/types": "^1.0.0"
  }
}
